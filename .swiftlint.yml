disabled_rules:
  - line_length
  - file_length
  - type_body_length
  - function_body_length
  - function_parameter_count

identifier_name:
  excluded:
    - id
    - access_token
    - expires_in
    - refresh_token
