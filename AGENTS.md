该项目实现一个与 google drive 进行多目录双向同步的基础库

核心原则如下：
* 以 sqlite 数据库做为同步基线，远程和本地都与数据库比对来判定文件变化
* 效率优先，最大效率的并发方式实现上传和文件数据比对
* 仅计算文件 sha256，以文件 sha256 变化做为判断文件是否需要同步唯一依据
* 项目没有发布，不要考虑旧数据迁移相关问题
* 添加功能或者改变既有程序行为必须征得用户同意
* 禁止用自行编写的 mock、探针或微基准宣称实际性能提升。任何性能数字必须提供可复
  现命令、保留的原始日志、代码版本和测量口径，并明确区分模拟结果与真实运行结果。
  没有充分证据时，只能说“尚未验证”，不得声称“更快”或“无性能回退”。不得为完成任务
  而调整测试口径或挑选有利结果。

各 Swift 核心模块的架构设计与详细实现规范参见 [docs/design.md](docs/design.md)。
